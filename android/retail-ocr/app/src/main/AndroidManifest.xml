<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Printing to the ZQ320 over Bluetooth.

        BLUETOOTH_CONNECT is a runtime permission from API 31. The legacy
        install-time permissions are capped at API 30 so they are not requested
        on newer devices, where they no longer exist.

        BLUETOOTH_SCAN is required even though this app never discovers
        devices. The Link-OS SDK calls AdapterService.cancelDiscovery()
        internally while opening a connection, and that call is scan-guarded on
        API 31+. Without this the connection fails with

            SecurityException: Need android.permission.BLUETOOTH_SCAN
            ... AdapterService cancelDiscovery

        which surfaces as a permission error for a permission that IS granted.

        neverForLocation is declared because we genuinely do not derive location
        from Bluetooth; it keeps the scan permission from dragging in
        ACCESS_FINE_LOCATION, which the operator would otherwise be asked for.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <!--
        Android 11+ package visibility.

        Every package ZebraGate looks up must be listed here. Without this the
        PackageManager lookup returns "not found" on EVERY device, including
        genuine Zebra hardware, so the device gate rejects everything and it
        looks like a hardware fault. See CLAUDE.md section 4 and PLAN.md R11.

        EMDK is listed but NOT required: verified against a real TC22 on
        Android 14, the EMDK runtime is not preinstalled. MX (com.symbol.mxmf)
        is the reliable platform indicator.
    -->
    <queries>
        <package android:name="com.symbol.mxmf" />
        <package android:name="com.symbol.datawedge" />
        <package android:name="com.symbol.emdk.emdkservice" />
    </queries>

    <application
        android:name=".RetailOcrApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RetailOcr"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ScanActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".ui.ResultActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

    </application>

</manifest>
