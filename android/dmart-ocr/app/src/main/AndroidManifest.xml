<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <!--
        Android 11+ package visibility.

        Every package ZebraGate looks up must be listed here. Without this the
        PackageManager lookup returns "not found" on EVERY device, including
        genuine Zebra hardware, so the device gate rejects everything and it
        looks like a hardware fault. See CLAUDE.md section 4 and PLAN.md R11.

        EMDK is listed but NOT required: verified against a real TC22 on
        Android 14, the EMDK runtime is not preinstalled. MX (com.symbol.mxmf)
        is the reliable platform indicator.
    -->
    <queries>
        <package android:name="com.symbol.mxmf" />
        <package android:name="com.symbol.datawedge" />
        <package android:name="com.symbol.emdk.emdkservice" />
    </queries>

    <application
        android:name=".DmartOcrApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DmartOcr"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ScanActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".ui.ResultActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

    </application>

</manifest>
